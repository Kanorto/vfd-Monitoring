- Переписан вывод в `vfd_monitor.py`: формирование экрана теперь идет через фиксированные 20-символьные строки и побайтовую кодировку служебных символов дисплея, из-за чего устранено смешивание строк, мерцание и разъезд символов.
- Добавлены безопасные функции отрисовки `write_screen`/`encode_vfd_text`, блокировка доступа к COM-порту и более надежное переподключение дисплея.
- Логика отображения метрик переработана: первая строка стабильно показывает CPU/GPU/RAM, вторая — скорости диска и сети, с компактным форматированием под 2x20 VFD.
- Добавлены гибкие переключатели метрик в трее: теперь можно отдельно включать и отключать CPU/GPU/RAM, температуры, диск и сеть, а строки VFD динамически пересчитываются и центрируются под текущий набор пунктов.
- Переработан алгоритм компоновки строк: сегменты автоматически уплотняются только при нехватке места, поэтому при отключении части метрик оставшиеся элементы корректно центрируются без ручной подгонки.
- Добавлен `requirements.txt` и GitHub Actions workflow `.github/workflows/release.yml`, который на теге `v*` собирает `VFD PC Monitor.exe` через PyInstaller и сразу публикует его в GitHub Release без использования artifacts.
- Добавлена полноценная подсистема автообновления из GitHub Releases: приложение хранит текущую и ожидаемую версии в конфиге, фоном проверяет релизы, скачивает свежий `.exe`/`.py` с повторными попытками и безопасно применяет обновление через отдельный `.cmd`-обновлятор при следующем запуске или выходе.
- После установки новой версии приложение сравнивает встроенную версию с `app_version` из конфига, пишет changelog в лог, сохраняет его для последующего просмотра и показывает краткое уведомление на VFD.
- В трей добавлено меню `Обновления` с ручной проверкой, принудительной установкой скачанного обновления и просмотром changelog, а на самом VFD появились анимированные статусы проверки/загрузки обновления с прогресс-баром.
- Автозапуск Windows теперь использует корректную команду запуска для `exe` и `py`-режима, поэтому обновлённый бинарник продолжает подниматься автоматически после замены файла.

- Репозиторий обновлений по умолчанию теперь жёстко задан как `Kanorto/vfd-Monitoring`, а после нескольких подряд неудачных попыток автообновления приложение показывает alert со ссылкой на релиз для ручного скачивания.
- Поведение трея доработано: после переключения отображаемых метрик, автозапуска и скорости меню принудительно обновляется через `icon.update_menu()`, чтобы приложение не завершалось и не теряло состояние при настройке пунктов `Что показывать`.
- На стартовом экране теперь второй строкой показывается текущая версия приложения, а компоновщик основных метрик сначала пытается использовать полные префиксы `CPU`/`GPU`/`RAM` и сокращает их только при нехватке ширины VFD.
- Выбор файла обновления переработан так, чтобы автообновление в первую очередь забирало любой `.exe` из релиза GitHub, даже если рядом опубликован файл с точным совпадением имени текущего `.py`.
- Добавлено пользовательское форматирование метрик через `vfd_config.json`: для CPU/GPU/RAM/диска/сети теперь можно задавать собственные шаблоны вывода, а для обеих строк — отдельно настраивать обычные и компактные отступы между блоками.
- В трей добавлены пункты `Открыть конфиг` и `Горячие клавиши`; приложение теперь умеет включать/отключать глобальные хоткеи и хранит их конфигурацию в разделе `hotkeys`.
- Реализовано окно записи горячих клавиш на `tkinter`: пользователь может нажать нужную комбинацию, приложение запоминает её, проверяет конфликты и сразу перерегистрирует хоткеи без ручного редактирования кода.
- Исправлено отображение статусов автообновления на VFD: обрезка и выравнивание строк теперь считаются по реальной ширине символов в кодировке дисплея, а прогресс скачивания показывается компактно в формате процентов и мегабайт без полосы из точек, чтобы первая строка не переползала на вторую.
- Стабилизирован процесс сборки Windows-релиза: `PyInstaller` вынесен в `requirements-build.txt`, добавлен скрипт `build_windows_release.py` с жёсткой проверкой на Windows + Python 3.11, а GitHub Actions переведён на эту сборку, чтобы не выпускать нестабильные `.exe`, собранные под Python 3.13.
- README дополнен инструкцией по безопасной сборке и пояснением ошибки `Failed to load Python DLL ... python313.dll`, чтобы было понятно, почему такой бинарник нужно пересобирать или заменять официальным релизом.
- Changelog в приложении переработан в полноценную историю последних 10 версий: список релизов подтягивается из GitHub Releases API, кэшируется в конфиге и показывается в отдельном окне с явным разделением по версиям, датам и ссылкам.
- Документация по сборке обновлена под реальный сценарий ручного выпуска релизов с локального Windows-ПК, а GitHub Actions теперь описан только как необязательный резервный путь.
- Выпущен релиз 1.0: приложение разделено на несколько Python-модулей (`vfd_monitor.py`, `notes_engine.py`, `notes_gui.py`), при этом сборка через PyInstaller `--onefile` сохранена, потому что импортируемые `.py` автоматически встраиваются внутрь одного `.exe`.
- Реализована полноценная система заметок: до 10 активных заметок одновременно, отдельный интервал показа для каждой, автоматический циклический вывод на дисплей и единый парсер/пагинатор текста для 2x20 VFD с последовательным показом длинных сообщений.
- Реализована подсистема напоминаний: разовые по дате и времени, ежедневные по времени и еженедельные по выбранным дням недели; активированные напоминания имеют приоритет на дисплее и остаются видимыми до скрытия или отметки о выполнении.
- Добавлен отдельный GUI управления заметками и напоминаниями на `tkinter` с двумя вкладками и историей действий, а также новые глобальные хоткеи для открытия окон, перелистывания элементов, скрытия и завершения.
- Проведена дополнительная модульная переработка под реальные категории приложения: горячие клавиши вынесены в `app_hotkeys.py`, а унифицированный рендер, расчёт ширины символов, форматирование и компоновка VFD-строк — в `vfd_display.py`.
- Исправлена унификация форматирования 2x20 VFD: компоновщик сегментов теперь учитывает реальную ширину символов дисплея, а не обычный `len`, поэтому строки с особыми символами и плотной компоновкой рассчитываются корректнее.
- Устойчивость трея усилена: меню теперь пересобирается через единый `build_main_menu`, а действия в меню обёрнуты безопасными callback-обработчиками, чтобы изменение настроек не приводило к самопроизвольному закрытию и поломке трея.
- Конфигурация `notes_reminders` сделана понятнее: добавлен раздел `settings` с лимитами и параметрами отображения (`max_active_notes`, `max_active_reminders`, `max_history_items`, длительности страниц и стандартный интервал заметок), а сами лимиты теперь реально применяются движком.

